// The following code fails to compile due to a missing variable declaration. Edit the code to have it compile and run successfully.

#include <stdio.h>

int main() {

    unsigned int variable = 10;
    printf("variable is %u\n", variable);
    return 0;
}

