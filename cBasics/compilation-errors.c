// The following program is supposed to print a line on the standard output but compilation fails due to several errors and should actually print 'This should work!'. Fix the errors in this file.

#include <stdio.h>

int main() {
    printf("This should work!\n");
    return 0;
}

